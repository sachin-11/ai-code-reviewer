# Base URL of the FastAPI service (server-side only, fetched from Next.js's
# Server Components, never exposed to the browser). Defaults to
# http://localhost:8000 if unset.
API_BASE_URL=http://localhost:8000
